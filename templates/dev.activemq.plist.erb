<?xml version="1.0" encoding="UTF-8"?>

<plist version="1.0">
  <dict>
    <key>Label</key>
    <string>dev.activemq</string>

    <key>Boxen</key>
    <dict>
      <key>Description</key>
      <string>ActiveMQ messaging queue</string>

      <key>Port</key>
      <integer><%= scope.lookupvar "activemq::config::port" %></integer>
    </dict>

    <key>ProgramArguments</key>
    <array>
      <string><%= scope.lookupvar "activemq::config::executable" %></string>
    </array>

    <key>RunAtLoad</key>
    <true />

    <key>KeepAlive</key>
    <true />

    <key>UserName</key>
    <string><%= scope.lookupvar "::luser" %></string>

    <key>WorkingDirectory</key>
    <string><%= scope.lookupvar "activemq::config::datadir" %></string>

    <key>StandardErrorPath</key>
    <string><%= scope.lookupvar "activemq::config::logdir" %>/console.log</string>

    <key>StandardOutPath</key>
    <string><%= scope.lookupvar "activemq::config::logdir" %>/console.log</string>
  </dict>
</plist>
